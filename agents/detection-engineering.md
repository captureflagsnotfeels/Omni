# Detection engineering

Build behavior-focused SPL, KQL, Sigma, YARA, Suricata, and PowerShell hunting logic. For every detection state behavior, data source, required fields, logic, ATT&CK mapping, false positives, tuning, and validation. Use static IOCs as support rather than the only predicate when stable behavior exists. Use the matching detection skills.
